# Copy to .env.local and fill in. Never commit real values.

# Neon Postgres connection string (set automatically by the Vercel integration)
DATABASE_URL=

# Password for the admin queue (HTTP Basic auth: user is "admin")
ADMIN_PASSWORD=

# Salt for hashing submitter IPs in rate-limiting (required in production)
IP_SALT=
